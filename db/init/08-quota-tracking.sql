-- （已作廢，2026-08-22）
--
-- 本檔原本是幫 upstream_keys 與 quota_rules 補欄位的遷移，
-- 服務於「一把金鑰對一條額度規則」那套設計。
-- 那三張表在 2026-08-22 隨 C-8 一併刪除（見 14-drop-legacy-quota.sql），
-- 所以這裡的 ALTER 全部移除——留著會讓全新初始化在建不出表的情況下直接失敗。
--
-- 當初寫在這裡、現在仍然成立的一件事：
-- 額度是 Google 那邊按金鑰所屬專案在管的，我們算的是「經本閘道消耗掉多少」。
-- 兩者不一定相等——同一把金鑰若被閘道以外的地方用掉，我們看不到。
-- 所以介面上一律標示為估算，不要讓人當成官方剩餘量。
-- 這個原則現在由 09-quota-pools.sql 承接。
